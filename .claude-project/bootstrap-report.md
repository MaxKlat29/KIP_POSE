# pose — Bootstrap Report

_Generated by `claude-project` finalize step at 2026-05-17T13:19:56Z._

## Pipeline run stats

- Steps completed: 7
- Transitions logged: 22
- Last failure (now recovered): feedback_loop — EOFError: EOF when reading a line

## Concept summary

# Concept — pose

**Profile:** ml  
**Created:** 2026-05-17  
**Schema:** v1  
**Initial pitch:** ML-Pipeline für 6D-Pose-Estimation von Bauteilen auf einer Tischplatte aus Top-Down-Bildern, mit Sim2Real-Training über Physics-Simulation und CAD-Modellen, für Roboterarm-Pick-and-Place. **Methoden-Stack (RGB-only): CNOS (Detection) + GigaPose (Coarse-Pose) + MegaPose (Refinement) + BlenderProc4BOP (Synth-Data) + Stable-Pose-Snap (Planar-Postprocessing).** Endprodukt: Top-Down-Bild → klassifizierte 3D-Modelle mit 6D-Pose auf simulierter Tischplatte (Three.js-Clickdummy).

> **Aktive Methoden-Entscheidung (2026-05-17):** RGB-only statt RGB-D. Hintergrund: vorhandene RGB-D-Daten sind nicht zuverlässig genug (Reflexionen, Metallteile, Depth-Noise) — wir wollen die Pipeline nicht auf instabile Depth-Maps stützen. Trade-off: ~10-15 AR-Punkte gegenüber RGB-D-SOTA (FoundationPose), kompensiert teilweise durch das starke Planar-Constraint (Objekt liegt auf bekannter Tischebene → effektiv 3DoF statt 6DoF). Stable-Pose-Snapping wird dadurch zum kritischen Stabilizer, nicht zum nice-to-have.

## TL;DR
Eine ML-Pipeline die aus einem Top-Down-Foto einer Tischplatte erkennt, welche bekannten Bauteile dort liegen und in welcher 6D-Pose — sodass ein Roboterarm die Teile greifen kann.

## Vision
> Eine ML-Pipeline die aus einem Top-Down-Foto einer Tischplatte erkennt, welche bekannten Bauteile dort liegen und in welcher 6D-Pose — sodass ein Roboterarm die Teile greifen kann.

## Problem
> Aktuel

_…truncated…_

## Roadmap summary

Total stories: **47**

- **P1** Foundation + Repo-Setup — target v0.1 — 5 stories
- **P2** Clickdummy v0 (Mock-Daten) — target v0.1 — 6 stories
- **P3** Synth-Data-Pipeline (BlenderProc4BOP) — target v0.1 — 6 stories
- **P4** Detection-Stage (CNOS) — target v0.1 — 5 stories
- **P5** Pose-Estimation (GigaPose + MegaPose) — target v0.1 — 5 stories
- **P6** Stable-Pose-Snap (Planar-Postprocessing) — target v0.1 — 5 stories
- **P7** Eval-Harness + Reports — target v0.1 — 5 stories
- **P8** Clickdummy v1 (Echte Daten) — target v0.1 — 5 stories
- **P9** Integration + Docs + v0.1-Release — target v0.1 — 5 stories

## Next steps

1. Open the roadmap in your browser: `gantt render && open .claude-project/roadmap.html`.
2. Start the first story: `project session` — picks the next ready story, creates the kanban ticket, and bridges them.
3. Track progress: the gantt chart auto-updates as you `gantt move <story-id> in_progress / in_review / done`.

---

_This report is a one-shot record of the bootstrap. The authoritative state lives in `.claude-project/roadmap.json` + `version.json`._
