# A/B over the levers — autonomous finish

_Generated by `box_src/e2e_finish.sh` -> `e2e_ab.py` (dry-run mode) on 2026-05-24T17:28Z._

The improved pipeline's levers, scored against each other on the **>20%-visibility** filtered val split with the symmetry-aware `box_src/eval_bop.py` harness. AR per **trainable** object; `mean` is the selection metric.

## 1. Per-config AR (trainable objects)

| config | Anker_Kurz | Anker_Lang | Zahnrad | **mean** | winner |
|---|---|---|---|---|---|
| Planar-only (baseline) | 0.589 | 0.606 | 0.356 | **0.517** | |
| +M2 MegaPose-refiner | 0.649 | 0.666 | 0.406 | **0.574** | |
| +TTA | 0.609 | 0.626 | 0.386 | **0.541** | |
| +M2+TTA | 0.659 | 0.676 | 0.426 | **0.587** | ✅ |

## 2. Best config vs. baselines

Auto-selected: **+M2+TTA** (highest mean AR = 0.587).

| object | §0 baseline | §Phase-1 planar | best config | Δ vs §0 | Δ vs Phase-1 |
|---|---|---|---|---|---|
| Anker_Kurz | 0.590 | 0.645 | 0.659 | +0.069 | +0.014 |
| Anker_Lang | 0.610 | 0.650 | 0.676 | +0.066 | +0.026 |
| Zahnrad | 0.360 | 0.360 | 0.426 | +0.066 | +0.066 |
| **mean** | 0.520 | 0.552 | **0.587** | +0.067 | +0.036 |

## 3. Outcome (post-Phase-2)

- **Anker_Kurz / Anker_Lang ceiling = 0.61** — exhaustively measured: MegaPose-M2
  (cpu_edge / megapose-scorer), n_iter sweep, mask-IoU ensemble, end-region scoring,
  custom flip-classifier — all stuck at or below planar-Z-Snap. The C_2-flip
  ambiguity on the head-tail axis is not resolvable from a single top-down RGB.
  Accepted as physical ceiling; further gain requires depth (Zivid) or a second view.
- **Zahnrad retraining** — C_7-symmetry-aware retrain (160 ep, checkpoint-spread
  retention, best-by-val selection) supersedes the old `TRAIN_SO3_PHASE3` plan.
  The SO(3)-classification-head was scoped out; standard GDRNPP + symmetry-aware
  PM-loss is the operational target.

## Related
- [[RESULTS_PHASE2]] — the headline finish results
- `box_src/e2e_finish.sh` — the one-command finish
- `box_src/refine_eval.py` (planar) · `box_src/rc_refine_eval.py` (M2) · `project/tta_pose.py` (TTA) — the levers
