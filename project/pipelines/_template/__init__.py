"""Template-Paket fuer eine neue, komplett andere Pose-Pipeline.

Kopiere dieses ganze Verzeichnis nach ``pipelines/<deine_pipeline_id>/`` und folge
``adapter.py`` + ``README.md``. NICHT direkt hier implementieren — `_template`
bleibt die unberuehrte Kopiervorlage.
"""
